@model IEnumerable<esercizioS17L1.Models.Pagamento>

<h2>Anagrafica Pagamenti</h2>

<table class="table">
    <thead>
        <tr>
            <th>Periodo</th>
            <th>Ammontare</th>
            <th>Tipologia</th>
            <th>Nome Dipendente</th> <!-- Aggiungi una colonna per il nome del dipendente -->
            <th>Cognome Dipendente</th> <!-- Aggiungi una colonna per il cognome del dipendente -->
            <th></th> <!-- Aggiungi una colonna per le azioni -->
        </tr>
    </thead>
    <tbody>
        @foreach (var pagamento in Model)
        {
            <tr>
                <td>@pagamento.Periodo.ToString("dd/MM/yyyy")</td>
                <td>@pagamento.Ammontare</td>
                <td>@pagamento.TipologiaPagamento</td>
                <td>@pagamento.Dipendente.Nome</td> <!-- Mostra il nome del dipendente -->
                <td>@pagamento.Dipendente.Cognome</td> <!-- Mostra il cognome del dipendente -->
                <td>
                    @Html.ActionLink("Dettagli", "Details", new { id = pagamento.ID }) |
                    @Html.ActionLink("Modifica", "Edit", new { id = pagamento.ID }) |
                    @Html.ActionLink("Elimina", "Delete", new { id = pagamento.ID })
                </td>
            </tr>
        }
    </tbody>
</table>

<p>
    @Html.ActionLink("Aggiungi Pagamento", "Create")
</p>
